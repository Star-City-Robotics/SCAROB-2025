package frc.State;

public class Sequence {

    public enum Action {
        SCORE,
        INTAKE
    }

    public enum GamePiece {
        CORAL,
        ALGAE
    }

    public enum Level {
        L1,
        L2,
        L3,
        L4
    }

    public enum State {
        HOME,
    }

    public enum Input {
        BEGIN,
    }
}
